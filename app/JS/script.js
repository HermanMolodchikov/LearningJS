
/*

let n = 0;
while(true) {
	n += 0.1;
	if(Math.abs(n - 0.3) < Number.EPSILON) break;
}
console.log(`Stopped at ${n}`);

function printToday() {
	var today = new Date();
	document.write(today.toDateString());
}

var names = ['german', 
				'anna', 
				'valera', 
				'shanti'];//new Array('german', 'anna', 'valera', 'shatnti');
	names[2] = 'Valeriy';
	names[4] = names[2];
	console.log(names);
	console.log(names[1]);
	
var luckyNumber = prompt('whole your lucky number?', '');
luckyNumber = parseInt(luckyNumber, 10);
	if (isNaN(luckyNumber)) {
		luckNumber = prompt('Please, say me your lucky  number.','');	
	}
document.write(names.length + '<br>');
var counter = luckyNumber;

while ( counter < names.length) {
	document.write(names[counter] + "<br>");
	counter++;// counter += 1, counter = counter + 1.
	
}	

for (var i=0; i <names.length; i++) {
	document.write(names[i]+ ' ');
}

document.write("<br>");

for (var j = names.length - 1; j >= 0; j--) {
	document.write(names[j]+ ' ');
}

*/
